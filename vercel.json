{
  "buildCommand": "npm run build",
  "functions": {
    "api/index.ts": {
      "includeFiles": "dist/**",
      "maxDuration": 60
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/admin(?:/.*)?", "dest": "/index.html" },
    { "src": "/(.*)", "dest": "/api" }
  ]
}
