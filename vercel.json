{
  "buildCommand": "npm run build",
  "functions": {
    "api/index.ts": {
      "includeFiles": "dist/**"
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/admin(?:/.*)?", "dest": "/index.html" },
    { "src": "/(.*)", "dest": "/api" }
  ]
}
